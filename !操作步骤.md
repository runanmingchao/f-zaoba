# Socratopia Web 使用指南

欢迎来到苏格拉底式的 AI 学习空间。这份指南会手把手带你从零开始，把这个网页应用跑起来。不需要任何编程基础，照着做就行。

---

## 准备工作：安装 Node.js

Node.js 是运行这个程序的底层环境，相当于你电脑需要先装好它，程序才能启动。

**1. 打开浏览器，访问：** https://nodejs.org

**2. 点左边那个绿色大按钮下载"LTS"版本。**

> LTS 是稳定版的意思，适合大多数人使用。

**3. 下载完成后双击安装包，一路点"Next"就装好了。**

> 过程中所有选项保持默认就行，不需要勾选任何额外的东西。

**4. 验证一下安装是否成功。** 按键盘上的 `Win + R`，输入 `cmd` 回车，会弹出一个黑底白字的窗口，这叫"命令行"。在里面输入：

```
node -v
```

回车后如果出现一行数字（比如 `v20.11.0`），说明装好了。如果显示"不是内部或外部命令"，说明安装没成功，重新装一次试试。

---

## 第一步（推荐）：一键初始化

把文件夹解压出来后，**双击里面的 `初始化.bat`**。

程序会自动完成全部准备工作（装依赖、建数据库、桌面快捷方式），你什么都不用管。跑完之后桌面上会多出两个东西：

- 📱 **Socratopia 学习空间** — 浏览器快捷方式
- 🚀 **启动学习空间.bat** — 以后双击它就能一键启动并打开浏览器

> 初始化只需做一次，以后直接双击 `启动学习空间.bat` 就行。

---

## 第一步（手动）：想自己操作也可以

如果上面的 bat 双击没反应，可以手动来：

**1. 打开命令行**：在文件夹上右键 → **"在终端中打开"**（Win11）或 **Shift+右键 → "在此处打开 PowerShell 窗口"**（Win10）

**2. 安装依赖**：输入 `npm install` 回车（约两分钟）

**3. 初始化数据库**：输入 `npx drizzle-kit push` 回车

**4. 启动程序**：输入 `npm run dev` 回车，等出现 `Ready: http://localhost:3456` 后浏览器打开这个地址

---

## 第五步：开始使用

打开页面后会自动跳到登录页。

### 第一次使用？先注册账号

点击页面下方的**"注册"**，填写三个信息：

- **邮箱**：任意邮箱地址（比如 `myname@test.com`，不需要真实邮箱，不需要验证码）
- **密码**：自己设一个密码（至少 4 位）
- **确认密码**：再输一遍刚才的密码

点"注册新账号"，自动进入学习空间。

### 以后再来？直接登录

输入之前注册用的**邮箱和密码**，点"进入图书馆"就行。

### 多个人用同一台电脑

每个人注册自己的账号就行（不同邮箱），互不干扰。聊天记录、教材、日记都是各归各的。

登录之后你会看到三位 AI 老师，选择一个就能开始聊天了。

### 课堂存档与恢复

聊天页面顶部有三个按钮：

- **下课**：上完一节课点它，课堂会被存档，下次打开自动回到这节课
- **新课**：开一节全新的课堂，当前的不会被删
- **历史课堂**：下拉查看最近的课堂记录，点一个随时切过去继续聊

你也可以在左侧导航栏点**"课堂记录"**，那里分两个标签页：

- **进行中**：还没下课的课堂，点"继续"回到课堂
- **已存档**：已经下了课的课堂，点"继续"会自动恢复并接着聊

---

## 万一遇到问题

**Q：打开浏览器显示"无法访问"？**
检查命令行窗口里是不是确实显示了 `Ready: http://localhost:3456`。如果还没有就多等几秒，第一次启动会慢一些。

**Q：`npm install` 报错？**
确保你的 Node.js 版本 ≥ 18（在命令行输入 `node -v` 查看）。如果版本低于 18，回上面的"准备工作"重新装最新版。

**Q：`drizzle-kit push` 报错？**
检查你是不是在项目文件夹里执行的（命令行的路径最后应该显示这个文件夹的名字）。不是在文件夹里的话，用第二步重新打开终端。

**Q：程序用久了变慢/占用空间变大？**
双击文件夹里的 `清理缓存.bat` 就行。它只删编译缓存（`.next`），不会碰你的教材、课堂、聊天记录。删完下次启动会自动重建。

**Q：以后每次用都要走一遍这些步骤吗？**
不用。最省事的方法：**双击文件夹里的 `启动学习空间.bat`**，自动启动服务并打开浏览器。只需要这一下。

手动方式：打开文件夹 → 终端输入 `npm run dev` → 浏览器打开 `http://localhost:3456`。

---

## 补充：更换安全密钥（可跳过）

程序自带了默认的安全密钥，只在你自己电脑上用完全没问题。如果你比较在意安全性，可以这样操作：

1. 在文件夹里找到 `.env.example` 这个文件
2. 复制一份，把副本重命名为 `.env.local`
3. 用记事本打开 `.env.local`，把里面两个 `=` 右边的值随便改成别的字符串，保存

搞定。
